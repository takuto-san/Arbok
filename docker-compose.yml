services:
  mcp-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: arbok-mcp-server
    volumes:
      - ${PROJECT_PATH}:/workspace
    env_file:
      - .env
    stdin_open: true
    tty: false
    restart: unless-stopped